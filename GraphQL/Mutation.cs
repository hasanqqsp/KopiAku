namespace KopiAku.GraphQL
{
    public class Mutation
    {
        // Define your mutation methods here
        // For example, you might have a method to create a new user or update an existing record
        // public async Task<User> CreateUserAsync(CreateUserInput input, [Service] IMongoDatabase database)
        // {
        //     var collection = database.GetCollection<User>("Users");
        //     var user = new User { Name = input.Name, Email = input.Email };
        //     await collection.InsertOneAsync(user);
        //     return user;
        // }
    }
}